name: Pages workflow disabled

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Pages deployment mode
        run: |
          echo "This repository should be published from Settings -> Pages -> Deploy from a branch -> main -> /root."
          echo "The old actions/deploy-pages workflow was disabled because deployments were failing."
